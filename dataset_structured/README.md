# Dataset Structured

Start here:
- `00_docs/README.md` for the overview
- `04_manifests/catalog.json` for the machine-readable catalog

Each dataset directory includes copied readable files plus a `dataset.json` metadata file.

Original raw files remain in the `_F/` and `_poi_converted_wgs (3).csv` locations.
