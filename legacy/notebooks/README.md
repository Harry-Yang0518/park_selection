# Legacy Notebooks

These notebooks are the original exploratory workflow and are kept for provenance only.

The maintained implementation is now in `src/park_selection/` and follows the final report methodology. In particular, the Python pipeline uses a 1.5 km catchment, Gaussian distance decay with `sigma = 750 m`, five-component equal-weight park quality, and the final optimization parameters from the report.
